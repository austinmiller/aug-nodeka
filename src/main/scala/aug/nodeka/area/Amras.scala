package aug.nodeka.area

import aug.nodeka.{Area, MobTrigger}

object Amras extends Area {
  override val mobTriggers: List[MobTrigger] = List.empty
  override val paths: Map[String, List[String]] = Map.empty
  override val name: String = "Amreas"
  override val keyword: String = "amras"
}
